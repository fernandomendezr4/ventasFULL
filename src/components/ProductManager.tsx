import React, { useState, useEffect } from 'react';
import EnhancedProductManager from './EnhancedProductManager';

interface ProductManagerProps {
  onProductSelect?: (product: Product) => void;
}

export default function ProductManager({ onProductSelect }: ProductManagerProps) {
                  </option>
  // Use the enhanced product manager with AI features
  return <EnhancedProductManager />;
}